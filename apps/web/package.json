{
  "name": "@gadx/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "check": "tsc -b --pretty false",
    "test": "node --experimental-loader ./test/runner-core-test-loader.mjs --experimental-strip-types --test test/*.test.mjs"
  },
  "dependencies": {
    "@gadx/runner-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}

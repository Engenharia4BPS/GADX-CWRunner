{
  "name": "gadx-cw-runner",
  "private": true,
  "version": "0.1.0",
  "description": "Araucaria DX web CW training runner",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @gadx/web dev",
    "build": "pnpm --filter @gadx/web build",
    "check": "pnpm -r check",
    "test": "pnpm -r --if-present test"
  }
}
